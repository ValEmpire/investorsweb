import React from "react";
import Layout from "../layout";
import MessagePageRoute from "../routes/message";

const MessagePage= () => {
  return (
    <Layout>
      <MessagePageRoute />
    </Layout>
  );
};

export default MessagePage;
