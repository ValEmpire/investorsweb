import React from "react";
import Layout from "../layout";
import ProjectViewRoute from "../routes/projects/project";

const ProjectViewPage = () => {
  return (
    <Layout>
      <ProjectViewRoute />
    </Layout>
  );
};

export default ProjectViewPage;
