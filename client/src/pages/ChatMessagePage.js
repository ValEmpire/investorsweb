import React from "react";
import Layout from "../layout";
import ChatMessageRoute from "../routes/chat";

const ChatMessagePage = () => {
  return (
    <Layout>
      <ChatMessageRoute />
    </Layout>
  );
};

export default ChatMessagePage;
